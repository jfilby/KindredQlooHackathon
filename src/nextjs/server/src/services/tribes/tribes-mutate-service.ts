export class TribesMutateService {

  // Consts
  clName = 'TribesMutateService'

  // Code
  async expandInterests() {

    // Debug
    const fnName = `${this.clName}.expandInterests()`

    // Get similar interests from Qloo
    ;
  }

  async generateTribes() {

    // Debug
    const fnName = `${this.clName}.generateTribes()`

    // Cluster interests
    ;
  }
}
